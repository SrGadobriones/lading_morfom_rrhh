# Endpoint del backend que recibe los leads del formulario "Solicitar demo".
# Si no se define, usa el default de src/config.ts (https://api.morfom.cl/public/leads).
PUBLIC_LEADS_ENDPOINT=https://api.morfom.cl/public/leads
